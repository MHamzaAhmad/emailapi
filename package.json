{
  "name": "email-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint"
  },
  "devDependencies": {
    "nx": "^22.3.3",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.26.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
