version: v2
name: buf.build/emailapi/public
deps:
  - buf.build/gnostic/gnostic
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX
    - PACKAGE_DIRECTORY_MATCH
breaking:
  use:
    - FILE
