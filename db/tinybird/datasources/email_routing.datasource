DESCRIPTION >
    Email routing table for reply threading and webhook routing.
    Maps email_id to message_id and user_id.

TOKEN emailapi_server APPEND
TOKEN emailapi_server READ

SCHEMA >
    `message_id` String `json:$.message_id`,
    `email_id` String `json:$.email_id`,
    `user_id` String `json:$.user_id`,
    `sent_at` DateTime `json:$.sent_at`

ENGINE "MergeTree"
ENGINE_PARTITION_KEY "toYYYYMM(sent_at)"
ENGINE_SORTING_KEY "email_id, message_id, sent_at"
ENGINE_TTL "toDateTime(sent_at) + toIntervalDay(90)"
